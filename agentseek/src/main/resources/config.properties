
# Player
player.noiseEmittingRadius=3.0
player.size=1.5
player.speed=3.5

# Agents
agents.camera.color=DARK_GRAY
agents.camera.rectangleSize=1.0
agents.camera.circlesRadius=0.5
agents.camera.wallAwareness=3.0
agents.camera.sightLength=15.0
agents.camera.sightApertureDegrees=45
agents.camera.excludedNames=[bound,obstacle,wall,bounds]
agents.camera.namesToTrack=[player]
agents.camera.standardLightColor=(255, 255, 0, 150)
agents.camera.dangerLightColor=(255, 0, 0, 200)

agents.guard.defaultColor=BLUE
agents.guard.defaultNoiseSensorRadius=4.0
agents.guard.defaultSightConeLength=10.0
agents.guard.defaultSightConeApertureRadians=1.2
agents.guard.defaultDistanceSensorRadius=2.25
agents.guard.size=1.5
agents.guard.nearBaseDistance=10.0
agents.guard.randomMovementTimerMillis=4000
agents.guard.sightTimerMillis=5000
agents.guard.noiseTimerMillis=5000
agents.guard.standardLightColor=(255, 255, 0, 150)
agents.guard.dangerLightColor=(255, 0, 0, 200)
agents.guard.maxSpeed=3.5
agents.guard.maxWanderingSpeed=1.5

agents.hearing.defaultColor=RED
agents.hearing.randomMovementTimerMillis=3000
agents.hearing.noiseTimerMillis=5000
agents.hearing.standardLightColor=(255, 255, 0, 150)
agents.hearing.dangerLightColor=(255, 0, 0, 200)
agents.hearing.defaultNoiseSensorRadius=6.0
agents.hearing.size=0.8
agents.hearing.maxSpeed=4.5
agents.hearing.maxWanderingSpeed=2.0

# Physics
physics.defaultMass=1.0

# GUI
gui.menuTitle=Agent Seek
gui.frameTitle=Agent Seek
gui.frameWidth=1000
gui.frameHeight=720

# Camera
camera.viewPort.width=50.0

# Rendering
rendering.defaultLayer=GENERIC
rendering.defaultColor=BLACK
rendering.backgroundColor=WHITE
rendering.textFont=Serif

# Names
player.name=Player
wall.name=Wall
door.name=Door

# Components
mouseEmitter.noiseRadius=5.0
mouseEmitter.noiseDurationMillis=1000

mouseEmitter.playerMaxDistance=15.0
noiseEmitter.speedThreshold=2.0

fieldMovement.rotationDegrees=115.0
fieldMovement.defaultMaxVelocity=3.5
fieldMovement.dangerCoefficient=1.0

sightSensor.defaultColor=YELLOW

# Visual components
sightSensor.visual.defaultSuspiciousTimeMillis=1000
sightSensor.visual.text=!
sightSensor.visual.textColor=BLACK
sightSensor.visual.fontSize=40

noiseSensor.defaultSuspiciousTimeMillis=3000
noiseSensor.visual.text=?
noiseSensor.visual.textColor=BLACK
noiseSensor.visual.fontSize=40
noiseSensor.visual.color=YELLOW

noiseEmitter.visual.color=BLACK
noiseEmitter.visual.durationMillis=500

mouseEmitter.visual.text=BUM
mouseEmitter.visual.fontSize=30
mouseEmitter.visual.textColor=BLACK
